import React from 'react';
import { Box, Heading } from '@chakra-ui/react';

const Dashboard = () => {
  return (
    <Box>
      <Heading>Dashboard</Heading>
      {/* Display user's projects and tasks here */}
    </Box>
  );
};

export default Dashboard;
